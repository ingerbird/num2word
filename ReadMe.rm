Android application intended to transform digit chunks into existing words in some language.
Scripts folder contains Python executables to sync database.

TODO:
  * procedure to actualize words from different context (books, articles, ...), calculate words weights (appearance count).